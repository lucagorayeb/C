# Array 

- O array é um conjunto de dados de mesmo tipo
  que fica armazenado na memória.

- Para declarar um array utiliza-se:
    int nome_do_array[n] (n pode ser qualquer número) 
        (O n determina a quantidade de elementos do array)